<%# render 'form', recipe: @recipe %>

<%= form_for @recipe do |f| %>
  Title: <%= f.text_field :title %>
  <%= f.fields_for :ingredients do |ingredient| %>
    Name: <%= ingredient.text_field :name %>
    Quantity: <%= ingredient.number_field :quantity %>
  <% end %>
  <%= f.submit %>
<% end %>
